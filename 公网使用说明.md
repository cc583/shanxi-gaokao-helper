# 公网访问说明

## 临时公网链接

双击 `启动公网网站.bat`，等待窗口里出现 `https://xxxxx.trycloudflare.com`。把这个链接发给外地的人，对方就能打开网站。

这个方式不需要买服务器，也不需要改路由器。缺点是电脑关机、休眠，或关闭启动窗口后，链接会失效。

## 后台运行

如果想让窗口关闭后仍然运行，可以使用 `后台启动公网网站.ps1`。停止时运行 `停止公网网站.bat`。

## 长期固定网址

长期使用建议部署到 GitHub Pages、Vercel、Netlify 或自己的服务器。这个项目是纯静态网页，上传整个文件夹即可。
